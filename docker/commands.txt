** LIST KAFKA TOPICS:
docker run -it --rm --network venttys-net -e KAFKA_CFG_ZOOKEEPER_CONNECT=venttys-zookeeper:2181 bitnami/kafka:latest kafka-topics.sh --list  --zookeeper venttys-zookeeper:2181

** CREATE COMMERCE CONTAINER
docker run -it --rm \
    -e PORT=80 \
    -e NODE_ENV=development \
    -e PHONE="+57xxxxxxxxxx"\
    -e COMMERCE="CommerceName-xxx" \
    -e EXTERNAL_PUBSUB_SERVER='kafka:9092' \
    --network=venttys-net \
    -p 8003:8003 \
    --name DemoTest \
    whatsapp-handler bash